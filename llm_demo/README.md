# ETL MV Agent-Only Prototype

This directory contains the first Agent-only prototype for the ETL MV Agent Prototype.

The first development slice implements:

- `SQLLoaderAgent`
- `FeatureAgent`
- `FamilyAgent`
- `SelfIterationAgent`

Later slices will add batch clustering, batch-local MV generation, rewrite, and execution.
